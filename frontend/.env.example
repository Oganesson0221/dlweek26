# Frontend Environment Variables
# These values are automatically loaded by Vite

# Backend API URL 
VITE_API_URL=http://localhost:8000

# OpenAI Configuration (stored in localStorage via Settings page)
# VITE_OPENAI_API_KEY is set from root .env and passed to the frontend build
